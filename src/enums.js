'use strict';

const CliCommandName = {
  Generate: `generate`,
  Help: `help`,
  Version: `version`,
};

const ExitCode = {
  Error: 1,
  Success: 0,
};

module.exports = {
  CliCommandName,
  ExitCode
};
